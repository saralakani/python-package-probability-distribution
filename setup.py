from setuptools import setup

setup(name='Sara_distributions_udacity',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['Sara_distributions_udacity'],
      zip_safe=False)
